void main()
{
    int num;
    int s;
    int index;
    float s; // redeclared identifier

    k = 0; // undeclared identifier
    num = index + 3.21; // type mismathc between + and = operator (two errors)
}